{
  "name": "feeds-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "typecheck": "tsc --noEmit",
    "check": "npm test && npm run typecheck && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
