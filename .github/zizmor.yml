# zizmor policy for trvny/feedseek.
# GitHub Actions are pinned to full commit SHAs. Enforce that posture so a
# future tag/branch pin cannot silently weaken the workflow supply-chain policy.
rules:
  unpinned-uses:
    config:
      policies:
        "*": hash-pin
